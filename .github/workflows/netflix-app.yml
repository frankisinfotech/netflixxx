name: netflix-app

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    # - name: Set up SSH
    #   run: |
    #     mkdir -p ~/.ssh
    #     echo "${{ secrets.SSH_KEY }}" > ~/.ssh/id_rsa
    #     echo "${{ secrets.VM_KEY }}" > private_key
    #     chmod 600 ~/.ssh/id_rsa
    #     chmod 600 private_key
        
    #     ssh-keyscan -H ${{ secrets.HOST }} >> ~/.ssh/known_hosts

    # - name: Deploy to VM 
    #   run: |
    #     # sudo rsync  -avz --delete ./ ${{ secrets.USERNAME }}@${{ secrets.HOST }}:${{ secrets.TARGET_DIR }}
    #     sudo rsync -avz --delete -e "ssh -o StrictHostKeyChecking=no -i private_key" ./ ${{ secrets.USERNAME }}@${{ secrets.HOST }}:${{ secrets.TARGET_DIR }}

    - name: Set up SSH key
      run: |
          echo "${{ secrets.SSH_KEY }}" > key.pem
          chmod 600 key.pem

    - name: Deploy to EC2
      run: |
          ssh -o StrictHostKeyChecking=no -i key.pem ${{ secrets.USERNAME }}@${{ secrets.HOST }} << 'EOF'
            sudo cp ./* /var/www/html
          EOF
